# Agent 扩展事件模型收敛 - 反思优化

> 创建时间：2026-04-24
> 选定方案：中策

## 一、边界情况

### 1. `message_received` 是否也应该改名

结论：**暂时保留**。

原因：

- 它本身已经是一个清晰的通知事件
- 语义没有歧义
- 没必要为了“全改成 started/completed”而硬改一个已经足够好的名字

### 2. `after_tool_call` 是否要补一个开始事件

结论：**当前不强制新增**。

原因：

- 现有 `prepare_tool_call` 已覆盖“调用前”的治理需求
- 当前最需要的是把“通知”和“拦截”分清，而不是追求事件绝对成对
- 如果后续确实有监听工具调用开始的稳定需求，再增加 `tool_call_started`

### 3. `session_start / session_end` 是否一定要替换成 `run_*`

结论：**应该替换**。

原因：

- 当前执行链里的触发时机并不是“会话实体被创建/销毁”
- 继续保留 `session_*` 只会让未来读代码的人误会

## 二、安全与稳定性评估

本次调整风险主要来自两类：

1. **代码级 rename 漏改**
2. **payload 语义变化导致测试失效**

由于当前扩展机制尚未正式使用，不需要对外兼容，因此这类风险主要是仓库内部可控风险。  
只要同步更新类型、调用点、测试和文档，整体风险可接受。

## 三、对现有功能的影响

### 正向影响

- 扩展 API 边界更清楚
- 压缩、Turn、Run、Tool 四类 Agent 生命周期更容易理解
- 未来做 extension 示例或 SDK 时，不需要先解释一堆历史命名

### 需要注意的影响

- `run_completed` 会吸收 `agent_end` 的职责，因此 `session_end` 应同步移除，避免重复事件
- 文档中凡是提到 `before_agent_start / after_tool_call / before_compaction` 的地方都要一起更新
- 如果 `ExtensionApi.on()` 暂时保留，类型提示必须已经分出 Event / Interceptor，不然外部感知不到这次收口

## 四、优化后的最终方案

### 1. 稳定公开面只保留两类能力

#### Agent Event

- `message_received`
- `run_started`
- `run_completed`
- `turn_started`
- `turn_completed`
- `tool_call_completed`
- `compaction_started`
- `compaction_completed`

#### Agent Interceptor

- `prepare_run_input`
- `prepare_tool_call`
- `transform_tool_result`

### 2. Runtime 内部事件移出默认公开面

以下点不再作为默认公开扩展能力：

- `message_queued`
- `message_dequeued`
- `queue_drain_start`
- `model_resolved`
- `model_fallback`

如后续确实需要，可单独设计为：

- `experimentalRuntimeEvents`
- `diagnosticEvents`
- 或内部专用调试事件流

### 3. API 逐步收口路径

#### 第一步：先完成类型和命名收敛

- 统一字符串名字
- 统一 payload 字段
- 在现有 `api.on()` 基础上完成收口

#### 第二步：再把注册入口语义化

从：

```ts
api.on('prepare_tool_call', handler);
api.on('run_started', handler);
```

逐步收为：

```ts
api.interceptors.use('prepare_tool_call', handler);
api.events.on('run_started', handler);
```

### 4. 建议的落地顺序

1. `types.ts` 中拆出 `AgentEventName` / `AgentInterceptorName`
2. 完成以下 rename：
   - `before_agent_start` -> `prepare_run_input`
   - `before_tool_call` -> `prepare_tool_call`
   - `tool_result_persist` -> `transform_tool_result`
   - `session_start` -> `run_started`
   - `agent_end` -> `run_completed`
   - `session_end` -> 删除
   - `turn_start` -> `turn_started`
   - `turn_end` -> `turn_completed`
   - `after_tool_call` -> `tool_call_completed`
   - `before_compaction` -> `compaction_started`
   - `after_compaction` -> `compaction_completed`
3. 将 runtime/provider 内部事件移出公开类型
4. 更新 `ExtensionApi` 和文档示例
5. 最后再考虑是否升级为 `events.on / interceptors.use`

## 五、最终结论

这次收敛不应该只停留在 `before_compaction` 改名。  
更合适的方向是：

- **用 Event 表达“发生了什么”**
- **用 Interceptor 表达“允许修改什么”**
- **把 runtime 内部信号从公开扩展面拿出去**

这样扩展系统才能真正成为“Agent 扩展机制”，而不是一个继续向外膨胀的万能 Hook 容器。
